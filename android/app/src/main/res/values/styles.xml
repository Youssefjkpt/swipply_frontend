<resources>
  <!-- 
    A MaterialComponents‐based “NormalTheme” so flutter_stripe can initialize. 
    (You can rename “NormalTheme” to “AppTheme” or whatever you prefer, but be
    sure to reference it in your manifest.)
  -->
  <style name="NormalTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <!-- preserve your black window background if you want -->
    <item name="android:windowBackground">@android:color/black</item>

    <!-- (optional) if you have your own color resources, you can set them here -->
    <!--
    <item name="colorPrimary">@color/your_primary</item>
    <item name="colorPrimaryVariant">@color/your_primary_variant</item>
    <item name="colorOnPrimary">@color/your_on_primary</item>
    -->
  </style>
</resources>
